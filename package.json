{
  "name": "seha-sa-inquiries",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp -r index.html css js images assets img api netlify data inquiry.php slenquiry.php vercel.json netlify.toml .htaccess *.zip dist/ 2>/dev/null || true",
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.8.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}